# CLAUDE.md — Guía Operativa para Agentes del Repositorio DemandSync

> Este repo lo usan **varias personas** (Equipo 207, UTN FRBA). Si sos un agente abierto por cualquier integrante, esta guía te da el contexto completo del proyecto y las reglas para trabajar sin pisar el trabajo de otros. Leela entera antes de tocar nada.

## 1. Qué es este proyecto

**DemandSync**: Sistema Inteligente de Predicción de Ventas para distribuidoras veterinarias. Proyecto Final de Ingeniería, UTN FRBA 2026, Equipo Nº 207. Es un **DSS de solo lectura**: ingiere datos del ERP del cliente y genera predicciones de demanda (1/6/12 meses), segmentación de clientes, recomendaciones de venta cruzada, alertas de vencimiento de lotes y borradores de orden de compra. No factura, no compra, no escribe en el ERP — solo sugiere; decide un humano.

**Primer cliente:** DFV, una distribuidora veterinaria real (ERP legado "defeve"). Todo el diseño de datos está validado contra su sistema real. El producto se diseña genérico, pero cada decisión se prueba contra este cliente.

**Estado actual: Release 0 (validación y diseño) para el producto, pero el motor ya tiene código.** La ingesta (R1) sigue bloqueada esperando que se congele el contrato de datos, así que backend, frontend e infra siguen en trabajo documental: contratos, planes y ADRs. El **motor** es la excepción por decisión explícita (ADR-009): accede a los datos por una interfaz de repositorio, así que se desarrolla y se evalúa contra archivos locales sin esperar la base — y de hecho ya tiene el arnés de backtesting, las métricas y el generador de dataset sintético funcionando. Ver `motor/roadmap-motor.md` para qué está hecho y qué falta.

## 2. Equipo y ownership — no pises el módulo ajeno

| Integrante | Rol | Módulo que le pertenece |
|---|---|---|
| Ian Feldman | Project Manager | `planning/` |
| Dan Judzik | Analista Funcional | `docs/` (contratos, casos de uso, validaciones) |
| Santiago Maffini | Backend Developer | `backend/`, `infra/` |
| Alejo Gurfein | Frontend Developer | `frontend/` |
| Ariel Habib | ML Specialist | `motor/`, `datasets/` |

**Regla:** trabajá en el módulo de quien te abrió. Si la tarea requiere cambiar un módulo ajeno o un contrato entre módulos (esquema de tabla, endpoint, formato de archivo), NO lo edites directamente: dejá el cambio propuesto documentado (issue, nota en `planning/`, o comentario en el doc del contrato) para acordarlo con el responsable. Los contratos entre módulos valen más que la conveniencia local.

## 3. Jerarquía de documentación (fuente de verdad, en orden)

1. **`docs/decisiones.md`** — ADRs. Las decisiones "Aceptadas" son vinculantes: **no las re-litigues ni escribas código/docs que las contradiga**; si hay motivo real para cambiar una, se registra un ADR nuevo que la reemplace.
2. **`docs/datos-defeve.md`** — qué datos existen DE VERDAD en el cliente 1 (y cuáles no). Cualquier diseño que asuma un dato que no figura ahí está mal.
3. **`docs/arquitectura.md`** — módulos, stack, flujo de datos, fronteras.
4. **`docs/vision-y-alcance.md`** — qué es y qué NO es el producto (límites explícitos).
5. **`motor/viabilidad.md` y `motor/plan-diseno.md`** — diseño del motor de predicción.
6. **`planning/roadmap.md`** — en qué release estamos, qué está pendiente y de quién.
7. **`docs/referencias/`** — historia de decisiones (docs heredados del trabajo con el cliente). **Solo lectura: nunca editar.** Ante conflicto entre un doc UTN formal y una corrección de `referencias/02_*`/`03_*`, gana la corrección (está validada contra el sistema real del cliente).

## 4. Regla de oro: datos del cliente

Este repo se comparte entre personas de dentro y fuera de la empresa cliente. Por lo tanto:

- **PROHIBIDO commitear datos reales del cliente**: ventas, clientes, precios, volúmenes, exports JSON, dumps, CSVs, o notebooks con outputs que los muestren. También credenciales, URLs internas, IPs o nombres de infraestructura del cliente.
- Los datos reales viven solo en la **máquina autorizada** (la del ML Specialist, que tiene acceso a la réplica del ERP). Al repo entran únicamente **métricas agregadas y conclusiones** (ej. tablas de error WAPE por categoría).
- El equipo desarrolla contra el **dataset sintético** de `datasets/sintetico/` (replica esquema y propiedades estadísticas, no registros). Si tu tarea necesita datos reales y no estás en la máquina autorizada, la salida correcta es: preparar el experimento para que lo corra el ML Specialist, no conseguir los datos.
- El `.gitignore` bloquea `datasets/*`; no lo debilites. Antes de cualquier commit que incluya archivos de datos o notebooks, verificá que no haya registros reales.

## 5. Contexto técnico rápido

- **Stack backend:** Python, FastAPI, SQLModel, Alembic, PostgreSQL 15 + pgvector (RAG). Batch nocturno; la API solo lee resultados materializados; consultas < 2s contra vistas mensuales agregadas.
- **Motor:** librería Python (no servicio HTTP) — `statsforecast` (baselines + intermitentes), `mlforecast` + LightGBM (modelo global), `hierarchicalforecast` (reconciliación). Invocada por el job batch; lee y escribe solo tablas.
- **Frontend:** a definir en R4 (no bloquea R1–R3).
- **Ingesta:** archivos JSON exportados del ERP según contrato congelado (ver `docs/datos-defeve.md`); deduplicación factura/remito obligatoria (ADR-003).
- **Conceptos clave del dominio que NO hay que violar:**
  - Hechos mensuales **inmutables y nominales**; nunca se persisten valores deflactados (ADR-001).
  - La deflación es read-time, con índice implícito por producto que **preserva el descuento individual** (ADR-002). Re-tasar `unidades × precio_hoy` está prohibido: borra el descuento.
  - Stock: solo foto actual; no existe histórico de movimientos (ADR-004).
  - `cluster_id` de segmentación jamás entra como feature de entrenamiento (ADR-005).
  - Todo resultado analítico se versiona por `EJECUCION_MODELO`.

## 6. Metodología de trabajo

- **Idioma:** español en docs, commits y nombres de dominio del código.
- **Plan antes de código:** para cualquier tarea que toque más de un archivo o cree estructura nueva, escribí primero un plan corto (qué archivos, qué cambios) y esperá confirmación de quien te abrió. Planes de etapa van en `planning/` con **fecha absoluta** (`**Fecha:** AAAA-MM-DD`) en el encabezado; al completarse la etapa, el plan se poda o se archiva actualizando `planning/roadmap.md`.
- **Control contra el roadmap del módulo** *(vigente hoy para `motor/` y `datasets/` → [`motor/roadmap-motor.md`](motor/roadmap-motor.md); se extiende a cada módulo cuando publique el suyo)*. Todo trabajo de desarrollo en el módulo se contrasta con su roadmap, antes y después:
  1. **Antes de empezar:** leé el roadmap y ubicá la tarea en una unidad de trabajo concreta (`T0.x`, `M1.x`, …). Si no encaja en ninguna, **no arranques**: o pertenece a un hito posterior, o falta la unidad de trabajo — en ese caso primero se agrega al roadmap (con entregable y gate de salida) y recién después se codea.
  2. **Respetá el orden y los gates:** no se empieza un hito si el gate de salida del anterior no está cumplido **con evidencia** (ver *Verificación*). Caso concreto en el motor: ningún modelo antes de que el arnés de backtesting y el piso de baselines estén congelados y commiteados.
  3. **Al terminar:** actualizá el estado de esa unidad en el roadmap del módulo **en la misma unidad de trabajo**. Prohibido dejarlo "para después" — misma disciplina que la documentación del TP.
  4. **Si el trabajo obliga a cambiar el plan** (reordenar, recortar, agregar o partir una unidad), el cambio se escribe en el roadmap con su motivo antes de seguir. Si además mueve un gate, una frontera entre módulos o un criterio de aceptación, va ADR.
- **Cómo se reporta el cierre de una unidad de trabajo:** tres partes explícitas y en este orden — **de dónde partiste** (el estado previo y el problema concreto que la unidad venía a resolver), **qué hiciste** (el cambio y su resultado medido) y **qué queda** (pendientes, con dueño). El punto de partida va primero aunque se haya discutido antes: sin él, los números no se entienden. Los hallazgos que se salen del alcance de la unidad van dentro de "qué queda" si generan trabajo, no sueltos al final.
- **Decisiones técnicas importantes** (regla de negocio descubierta, cambio de contrato, elección de librería estructural) → ADR en `docs/decisiones.md` (formato: contexto / decisión / consecuencias, con estado `Propuesta` hasta que el equipo la ratifique).
- **Documentación del TP siempre al día:** toda decisión de negocio que se tome o cambie (regla de negocio, alcance, métrica, contrato de datos) DEBE reflejarse en la misma unidad de trabajo en la documentación formal del trabajo práctico — los documentos UTN afectados (Acta, DER, Casos de Uso, Plan de Pruebas) y sus espejos en `docs/`. Está prohibido dejar la documentación divergente del acuerdo vigente "para después". Concretamente: cada ADR nuevo o modificado debe cerrar con una línea **"Docs impactados:"** listando qué documentos hay que actualizar, y la tarea no se considera terminada hasta que esa actualización esté hecha (o registrada como pendiente asignado en `planning/roadmap.md` si el doc formal lo edita otro rol).
- **Commits:** `<tipo>: <descripción imperativa, español, máx 72 chars>` — tipos: `feat`, `fix`, `docs`, `refactor`, `chore`, `test`. Agregá archivos explícitamente (no `git add -A`). No commitees trabajo a medias que rompa el build.
- **Verificación:** nada se declara "listo" sin evidencia ejecutada (test corrido, endpoint curleado, métrica calculada). En el motor rige además la disciplina **baselines-first**: ningún modelo se promociona si no le gana a los baselines en backtest (ver `motor/plan-diseno.md`).
- **No inventes datos del cliente:** si te falta un dato de negocio (¿existe tal campo? ¿cuántos meses de historia hay?), la respuesta está en `docs/datos-defeve.md` o hay que preguntarle al ML Specialist / Analista. No asumas.

## 7. Estado de la Misión

*(Instrucción para el agente: al cerrar una sesión de trabajo relevante, actualizá SOLO esta sección con el estado y los pendientes accionables, con fecha. Mantenela corta — máx ~15 líneas; el detalle va en `planning/roadmap.md`.)*

**Última actualización:** 2026-08-06 — **M2 CERRADO: el gate está cumplido, y lo cumple el champion/challenger — no el modelo global solo (ADR-017).** M2.5 puso a competir a los 9 candidatos por (serie, corte) y el titular de M2.3 —"el global gana 11 de 12 celdas"— resultó estar tapando dos cosas: el **WAPE agregado es 86% del cuadrante `suave`**, y ahí donde no pesa, el global es **2 a 3 veces peor que el baseline** (`lumpy` h=12: WAPE 5,63 contra 2,55, con sobre-pronóstico del +511%). Son el **31% de los productos** y el **0,6% del peso**. El global solo **no cumple el gate** (pierde a h=12 contra el piso); el champion gana los cuatro horizontes y los baselines se quedan con el **84%** de los pares. **Sigue M3.1: reconciliación jerárquica.**

- Release: **R0** para el producto. **R1 (ingesta) sigue bloqueado por el contrato** (P1–P4); el motor ya no lo espera (ADR-009).
- Docs base + **ADR-001..016**. Aceptadas: 007 (unidades), 008 (WAPE/MASE/sesgo), 010 (demanda cero), 012 (universo), 013 (features de precio), **016 (piso prospectivo)**. **En Propuesta: 009** (Backend Dev), **011** (IPC empaquetado), y **014/015** (ver abajo).
- **M1.9 (2026-08-05, ADR-016) — el piso estaba inflado y el sesgo que motivó ADR-015 era del criterio de medición.** La selección retrospectiva (`elegir_mejor_por_serie`) elegía el modelo de cada serie con el MASE de **todos** los cortes y lo aplicaba también a los viejos. Re-congelado con **selección prospectiva** (ganador por (serie, corte), solo con error cuyo `anio_mes <= corte`) + **cascada por disponibilidad**: WAPE producto **0,287 → 0,331** (h=1), cobertura h=12 **0,888 → 0,910**, y sesgo total de **−5,2%/−6,0%** (h=6/h=12, fuera del ±5%) a **−1,0%/−0,9%: adentro en los cuatro horizontes**. **Más de la mitad de esa corrección es anterior a la cascada**, así que no es efecto de cobertura. El piso vigente es `baselines-real-prospectivo-2026-08-05.md`; el del 2026-08-03 queda como registro histórico y **no se comparan entre sí**.
- **Tres cosas de M1.9 que no estaban previstas:** (a) **la cascada no era opcional** — sin ella la selección prospectiva *empeora* la cobertura (0,865 contra 0,888 a h=12), porque reelegir por corte deja más series con un ganador incapaz de cubrir el horizonte; (b) lo que queda sin cubrir son **12.700 filas, exactamente las altas de catálogo de §5.6.1** — el componente reparable se cerró entero y M2.5 ya puede comparar a igual cobertura; (c) **el hindsight no se agota con la evidencia**: la brecha sigue abierta en el último corte, con 17 de historia, y solo el **15,7%** de las series conserva el mismo ganador en los 18 cortes.
- **Costó 12 segundos porque los checkpoints estaban en disco** (reusa la corrida `a79a9b23676b`, cero modelos reajustados); sin ellos eran 294 min. **`C:/dfv-checkpoints-2026-08-03` no se borra.** El `id` no incluye `n_jobs`, así que el mismo comando con `--checkpoint-dir` reanuda sin predecir nada.
- **Auditoría contra `docs/Aprobado/` (2026-08-05):** el motor divergía de documentación **aprobada** en tres puntos y solo uno tenía ADR. Cerrados dos: **ADR-014** (el Acta, CU-03 y CU-09 comprometen clima; el motor no lo modela — y el dato del MVP es **mock por contrato §6**, así que entrenar sobre él no puede dar señal) y **ADR-015** (el compromiso del producto se acota por horizonte, invocando el **Riesgo 5 del Acta** que ya lo pre-autorizaba) — pero ⚠️ **ADR-015 hay que revisarlo antes de ratificarlo: M1.9 le corrigió la evidencia el mismo día** (el piso prospectivo cumple el ±5% en los cuatro horizontes). Se cae su argumento del sesgo; queda en pie y reforzado el de la **varianza**, porque el WAPE empeora justo en horizonte largo. El tercero, **MAPE**, tiene ADR-008 desde el 2026-07-25 y sigue **⬜ en el Analista**: el Acta, CU-03, la Matriz de Riesgos y la tarea 55 del Gantt piden MAPE y el motor mide WAPE/MASE/sesgo. **ADR-015 punto 4 le da reemplazo al badge de CU-03** (el intervalo P10–P90), que era lo que no tenía dueño.
- **Y el Gantt aprobado ordena el trabajo al revés que el motor:** su línea base arranca clustering RFM el 2026-08-04 y series temporales el 08-25; el motor fue baselines-first y tiene el piso congelado, con RFM en M3.3 (S11). **Adelantado en series temporales, atrasado en clustering.** Pedido de rebaseline registrado en `planning/roadmap.md` para el PM.
- **EDA sobre datos reales** ✅ (`motor/eda/eda-2026-07-15.md`): 96 meses desde 2018-07, cuadrantes 48/31/10/11%.
- **Contrato de ingesta v0.9** (`docs/contrato-ingesta.md`): el cliente entrega ventas unificadas; DemandSync valida garantías y no re-deduplica.
- **Motor — hecho** (detalle y gates en `motor/roadmap-motor.md`): **S0, M1 y M2 COMPLETOS** — arnés rolling-origin con checkpointing reanudable, métricas ADR-008 por nivel con cobertura, clasificador de cuadrantes, los 7 baselines, las **dos** selecciones (retrospectiva y prospectiva), el extract del snap, **el piso vigente** (`baselines-real-prospectivo-2026-08-05.md`), la deflación, las features, **el modelo global LightGBM**, sus **intervalos P10/P50/P90** y el **champion/challenger**. **367 tests verdes**, `ruff` limpio.
- **⚠️ `lightgbm` está topado en `<4.7` y no es capricho:** con 4.7.0 en Windows, `fit` muere con `OSError: access violation` si **`pyarrow` se cargó antes** — o sea siempre, porque la capa de datos lee parquet. No es de los datos (falla con arrays contiguos, sin `category` y con `n_jobs=1`) y `KMP_DUPLICATE_LIB_OK` no lo arregla. `tests/test_entorno_lightgbm.py` lo corre **en subproceso**, que es la única forma: es un crash del binding C, no una excepción de Python.
- **M2.3 cerrada (2026-08-06, §6.5) — el global le gana al piso en 11 de 12 celdas nivel×horizonte**, con **cobertura idéntica fila a fila** (las mismas 12.700 filas sin cubrir: altas de catálogo que nadie puede predecir). WAPE producto **0,3305 → 0,2953** (h=1), total **0,1205 → 0,0934**; pierde solo en producto h=12 (+0,0047). Sesgo dentro del ±5% en los cuatro horizontes. **Con los agregados el gate de M2 está**; falta que M2.5 lo confirme por serie. Corre en **~4 min** las 18 cortes: el caro de M1.8 era `AutoARIMA`, no el modelo.
- **Configuración del global, medida y no elegida:** **`precio+crudo`**. Las features de precio de M2.2 valen **0,0233 de WAPE a h=12** (6% relativo) y su aporte **crece con el horizonte** —a h=1 son irrelevantes, a 12 meses los lags ya no dicen nada—; `LocalStandardScaler` **empeora 32%** a h=12, porque le saca al global la escala que usa para agrupar productos parecidos.
- **⚠️ La ablación de precio sobre el sintético da el signo OPUESTO, y no es contradicción:** `datasets/sintetico/demanda.py` no mira el precio, así que ahí esas features son **ruido exacto**. Cualquier ablación de features sobre el sintético mide, como mucho, si el modelo tolera ruido. **No saquen conclusiones de negocio del sintético.**
- **La tabla sintética de M1.7 caducó y nadie lo había notado (§6.4):** se congeló el 2026-07-30 y **T0.4 reescribió el generador al día siguiente**. Misma muestra y semilla, otro dataset (38.095 filas contra 27.683). El `id` de corrida lo detecta —para eso está— pero solo si alguien lo mira. **Una tabla congelada sobre el sintético caduca cuando cambia el generador; las de datos reales no.**
- **M2.4 cerrada (2026-08-06, §6.6) — el intervalo P10–P90 calibra solo, y el promedio esconde por qué.** Cobertura empírica **0,7798 / 0,8199 / 0,8130 / 0,8085** contra el 0,80 nominal, con los mismos hiperparámetros sin tunear de M2.3 (`objective="quantile"`). **No se tuneó para llegar: el gate pedía reportar la cobertura, no alcanzarla.** Por cuadrante se abre: `suave` 0,80 clavado, **`erratica` 0,67–0,70** (sub-cubre: promete menos riesgo del que hay), `intermitente`/`lumpy` sobre-cubren con **amplitudes de hasta 13x el real**. El agregado calibra porque `suave` es la mayoría de las filas. **Costo 19,4 min** (94 s/corte contra 21,6 del punto solo: 48 ajustes por corte).
- **Tres cosas de M2.4 para el que siga:** (a) **el cruce de cuantiles no es problema** — `P10 > P90` en **1 fila de 105.890**, y reordenar mueve la cobertura en la cuarta decimal, así que **M4.1 puede escribir `limite_inferior/superior` sin paso de reordenamiento**; (b) **el P50 le gana a la media a h=12** (0,3627 contra 0,3746) y esa es **justo la única celda que M2.3 perdía** contra el piso (0,3699) — entra a **M2.5 como candidato**, pero elegir el estimador por horizonte mirando la tabla final es hindsight: va con la regla prospectiva de ADR-016; (c) el WAPE del punto **reproduce exacto** el de M2.3 porque los cuantiles se suman al mismo `MLForecast` — hay test de igualdad exacta que lo fija.
- **⚠️ La corrida de intervalos necesita `--checkpoint-dir` propio.** El `id` no incluye el predictor, así que comparte `id` con las ablaciones de M2.3 y habría reanudado checkpoints sin columnas de cuantil **sin avisar**. El de M2.4 es `C:/dfv-checkpoints-intervalos`.
- **Y no uses el contador de CPU para saber si una corrida avanza:** `Get-Process().CPU` y `Win32_Process` dieron **~0 s y 11 MB** para el proceso mientras entrenaba normalmente. Sobre esa lectura se diagnosticó un cuelgue inexistente y se bajó una corrida sana. **La señal son los checkpoints en disco**, con su marca de tiempo.
- **M2.5 cerrada (2026-08-06, §6.7, ADR-017) — lo promocionable es el champion, y el global solo no lo era.** WAPE producto contra el piso (0,3305/0,3767/0,4001/0,3699): global **0,2953/0,3435/0,3834/0,3746** —pierde h=12— y champion **0,3230/0,3667/0,3928/0,3644**, que gana los cuatro. **Costó 45 s y cero modelos reajustados:** los checkpoints de las dos corridas comparten `id` y se cruzan fila a fila (`motor.backtesting.checkpoints`); rehacerlas eran 294+19 min.
- **El hallazgo de M2.5 vale más que el número: el WAPE agregado es 86% `suave`.** `suave` carga el 85,9% de la suma de `|real|`, `erratica` 13,3%, e `intermitente`+`lumpy` **0,6% entre los dos siendo el 31% de los productos**. Ahí el global sobre-pronostica y **empeora con el horizonte** (sesgo `lumpy` +0,59 a h=1 → **+5,11** a h=12): predice la media condicional de productos parecidos y en una serie a ráfagas esa media está muy por encima del mes típico. **Ninguna decisión de promoción se toma con el agregado solo** — se mira el desagregado por cuadrante **con su columna de peso**.
- **Tres cosas más de M2.5:** (a) **el P50 quedó descartado como estimador puntual** — lo que §6.6 dejó abierto: gana 3.512 turnos de selección (más que `GlobalLGBM`) pero mueve el WAPE 0,001, y su **sesgo −8,4% a −13,4% viola ADR-008**; sigue como candidato del champion; (b) **sumar candidatos no compra nada estructural** — mismas 12.700 filas sin cubrir y **las mismas 335 series** estables (15,7%) con 7 o con 9; (c) **una serie que un modelo no predijo saca WAPE 0,0 —perfecto— y solo `cobertura` lo delata**, así que comparar "donde los dos tienen WAPE definido" corona al que no predijo: la comparabilidad exige cobertura **>0 e igual**.
- **ADR-018 (Propuesta, 2026-08-06) reemplaza a ADR-015 y cierra las tres decisiones que estaban abiertas para el PM** — eran la misma pregunta desde tres lados. **El compromiso de precisión se expresa por cuadrante, no por horizonte:** a horizonte fijo el cuadrante mueve el error **4 a 9 veces**; a cuadrante fijo el horizonte lo mueve **1,1**. Consecuencia práctica: la advertencia de CU-03 tiene que dispararla el **cuadrante del producto**, no el horizonte — hoy avisaría en un `suave` a 12 meses (WAPE 0,32) y callaría en un `lumpy` a 1 mes (1,21). El criterio de aceptación del R2 pasa a **total y categoría**, donde está medido y cumplido. En `planning/roadmap.md` quedan **2 filas en vez de 4**.
- **Antes de escalar esas decisiones se verificó que no fueran un bug, y la respuesta fue "no" con razón (§6.9).** La calibración conformal prospectiva sobre los checkpoints de M2.4 (minutos, cero reajustes) levanta `erratica` de 0,679 a 0,787 **a h=1 y nada a h=12** —a doce meses casi no hay error observado con que calibrar sin violar ADR-016—, **no puede tocar `intermitente`** porque el 81,4% de sus filas tiene `real == 0` y `P10 == 0` y el score de conformidad es exactamente 0 (átomo en cero: no hay cuantil que encoja el intervalo), y **empeora `suave`**. La dispersión entre cuadrantes no es un defecto reparable: es la señal. Deuda anotada en §12.0. **Regla: antes de escalar una decisión, gastá los minutos en comprobar que no sea tuya.**
- **⚠️ El sintético de M2.5 habría promocionado el global (§6.7 punto 6).** Ahí gana 3 de 4 horizontes —incluido h=12, la celda donde en real pierde— porque el generador **no reproduce las ráfagas** de `lumpy`/`intermitente`: 1,455 vs 1,368 del piso, contra 5,63 vs 2,55 en real. **Replica** que el champion le gana al piso; **no replica** el modo de falla que decide la promoción. El sintético es buen smoke y mal árbitro.
- **Evaluación de cierre de M2 (§6.8) — el número honesto de la fase es 1,5%–2,6%, no 10,66%.** Esa es la ganancia del **champion** (lo que se entrega) sobre el piso a grano producto; el 10,66% era el global solo, que no resultó promocionable. A nivel total da 7–10%, pero el producto sugiere órdenes **por producto**. No es mal resultado: el piso ya eran 7 candidatos con selección prospectiva. Es un resultado **caro**, y no hay que venderlo como un salto.
- **La palanca más grande que M2 deja sin usar, y es lo primero a medir en M3:** el champion elige por `(serie, corte)` —~2.100 decisiones por corte con poca evidencia cada una— y eso cuesta varianza: en `suave` es **peor que el global solo** (0,2954 contra 0,2654). Elegir por **`(cuadrante, corte)`** son 4 decisiones y usa la estructura que las tablas muestran: **`suave` → global siempre; los tres cuadrantes irregulares → P50 casi siempre.** Cota estimada **con hindsight** (techo, no resultado): el champion deja **8,7% / 7,9% / 4,9% / 2,3%**. ⚠️ **No es "M1.7 se equivocó":** M1.7 midió que un **ruteo fijo por teoría** (intermitentes → Croston) era peor; esto es **selección aprendida** más gruesa. Medir con regla prospectiva antes de adoptar.
- **Y el P50 tiene un nicho estructural que ADR-017 acota:** en `intermitente`/`lumpy` es **más preciso Y menos sesgado** que el piso y que la media (h=1: WAPE 0,586 y sesgo −9,4%, contra 0,960/+21,1% del piso y 0,960/+45,7% del global). Su descalificación por sesgo es un veredicto de `suave` —el 86% del peso—, no una propiedad del estimador en todo el catálogo.
- **❌ M3.0 cerrada con resultado NEGATIVO (2026-08-07, §6.10) — las features de dispersión no eran el problema, y el experimento enseñó más que si hubiera salido bien.** Hipótesis: `erratica` se separa de `suave` solo por el CV² y el modelo no tenía ninguna medida de dispersión, así que no las distinguía. Se agregó `RollingStd` + CV (3/6/12) y **compra entre 0,1 y 2,3 puntos contra una brecha de 10 a 13** — no cierra nada — **y derrumba `intermitente`/`lumpy` a h=1**: cobertura 0,857 → **0,386** y 0,709 → **0,393**. Mecanismo verificado: el `P10 == 0` exacto cae del **82,1% al 31,4%** de las filas, o sea que las features le dieron señal para levantar el piso del intervalo **justo donde cero era lo correcto** (con panel densificado, la fila más frecuente de una serie intermitente es `real == 0`). **La dispersión ya era inferible de los lags**: no era un problema de información. `usar_dispersion` queda en `False`; el código y la tabla se conservan para que el negativo sea reproducible.
- **✅ La sub-cobertura de `erratica` quedó CERRADA como irreducible (2026-08-07, §12.0) — no hay nada que reparar.** Cuatro hipótesis probadas y descartadas: falta de información (M3.0), post-proceso (§6.9), que el intervalo no escalara con el tamaño (`ancho/escala` ≈ 0,8–1,4 en todos los deciles) y los hiperparámetros. **El test que la cierra:** cobertura **dentro** de la muestra de entrenamiento contra la de afuera, un solo ajuste, **1,2 min**. `erratica` da **0,7782 sobre datos ya vistos** —a 2,2 puntos del nominal— y **0,6790** a futuro; `suave` cae 3,5 puntos en la misma transición y `erratica` 9,9. **De los 12 puntos, ~2 son del modelo y ~10 son que la dispersión futura de una serie errática no está en su pasado.** Tunear hiperparámetros recuperaría ≤2 puntos, y más capacidad **empeoraría** lo no visto. **No se abre unidad; el barrido de capacidad que parecía obvio habría sido contraproducente.** Refuerza ADR-018 punto 2: publicar el 0,679 es informar una propiedad del negocio, no conformarse con un defecto.
- **⚠️ Trampa de método que casi arruina ese test:** el arnés **densifica a ceros (ADR-010) antes de entrenar** y el script del diagnóstico no lo hacía — 13,6% de ceros en entrenamiento contra 81,5% a futuro, o sea otro modelo. Se detectó **comparando la composición de las dos muestras antes de interpretar el número**. Toda medición dentro-vs-fuera tiene que verificar primero que las muestras sean comparables; si no, mide la diferencia de muestreo y la llama hallazgo.
- **Dos reglas que deja M3.0:** (a) **el `P10 == 0` de los intermitentes es frágil y hoy funciona** — cualquier cambio de features tiene que re-verificarlo, no alcanza con mirar el agregado; (b) **poner el interruptor apagado por defecto fue lo que salvó las tablas de M2** — el `id` de corrida no incluye las features, así que con el default encendido M2.3/M2.4/M2.5 habrían empezado a dar otros números bajo el mismo `id` y la única señal habría sido una sub-cobertura atribuible a cualquier cosa. **Feature nueva = interruptor apagado + corrida propia de checkpoints.**
- **Motor — sigue:** **M3.1** (reconciliación jerárquica). El extract canónico es **`C:/dfv-extract-v2`** (fuera del repo, son datos reales); `C:/dfv-extract` es el viejo y `-v3` una prueba con umbral $1 — **correr contra el directorio equivocado no falla, da otro número**. Regenerarlo requiere el túnel SSH de cotizaciones arriba (`docker compose --profile dev up`) y `pip install -e "motor[extract]"`.
- **M2.2 cerrada (2026-08-04) — ADR-013: la mitad de las features de precio que pedía el plan eran identidades algebraicas.** `precio_prom × deflactor = ancla` (99,15% de las filas, CV intra-producto **0,0000**) y `revenue_real = unidades × ancla` (99,13%): a grano producto el monto deflactado **es el target reescalado**, no señal. La señal es el **precio relativo a su nivel** (CV 0,1511, cobertura 98,99%). Se sacó también `mismo_mes_año_anterior` (a grano mensual **es** `lag 12`) y **`CLIENTE_FEATURE` se difirió a M3.2**: el extract real no tiene cliente×producto, así que construirla ahora dejaría a M2.5 comparando una corrida real con una feature menos que la sintética. Los lags los ejecuta `mlforecast`; M2.2 solo los especifica. **A grano cliente el monto deflactado sigue valiendo** (CP-INF-04 / RFM de M3.3): ahí el descuento individual sobrevive.
- **Dos fixtures de M2.2 pasaban en verde sin probar nada, y los cazó la mutación:** uno usaba un producto que crecía **igual que su categoría** (serie plana → alinear por filas o por calendario daba el mismo número) y el otro un producto **sin ancla** (el `NaN` venía de ahí, no de la máscara que decía probar). **Un fixture mal armado no falla: confirma.**
- **M2.1 cerrada (2026-07-31):** `motor/src/motor/deflacion/` + el IPC del INDEC empaquetado (`motor.datos.ipc`, dato público, CC-BY). **235 tests**, `ruff` limpio. Cobertura de ancla **73,2% propia** contra el 74,6% que esperaba el EDA §4. La validación que más convence: deflactando el extract, el revenue anual real queda **plano entre 36.000 y 40.700 M de 2019 a 2025** mientras el nominal se multiplica por 29. Corre en 2,1 s sobre 137.399 filas. **Las constantes se midieron, no se eligieron** (`LIMITE_RELATIVO=3`: con 2 el clamp recorta la devaluación de dic-2023; con 3 deja de reaccionar a eventos macro).
- **Una cosa de M2.1 para el que siga:** el peldaño **laboratorio lo usa 1 producto** de 2.128 — los datos reales no lo ejercitan, se testea a mano o queda sin cubrir. *(El otro pendiente, el deflactor directo sin acotar, se cerró el 2026-08-02 — ver abajo.)*
- **M1.8b (2026-08-02, ADR-012) — el universo estaba mal.** Tres hallazgos: (a) los **obsequios** se facturan con un centinela de **$0,01** (el ERP exige `precio > 0`) y nunca se filtraron — se cortan **por renglón**, no por producto, porque el flag `producto.obsequio` marca 12 productos que **venden a precio real** y los 48 marcados cargan 0,92% del revenue; (b) el "." de los **descontinuados** vive en `descripcion`, no en `id`, y es subconjunto exacto de `disabled` — que **no tiene fecha**, así que aplicarlo hacia atrás es sesgo de supervivencia (184 productos vivos al corte 2024-12); (c) **la réplica del snap se atrasa**: "último mes completo" es calendario, no datos. Universo **2.189 → 2.128**. **248 tests**, `ruff` limpio.
- **El residuo del deflactor quedó cerrado (2026-08-02):** `LIMITE_DESVIO_NIVEL = 10` acota `q = d/d_nivel` contra **categoría y laboratorio, nunca contra el IPC**. La distinción es lo que evita contradecir a ADR-002 — la cascada *estima un precio que falta*, esto *valida uno observado* — y es lo que salva CP-INF-01, cuyo fixture usa la rama IPC. Deflactor máximo **13.821 → 319**, cero filas por encima de 1.000, revenue real total −0,32%. **250 tests.**
- **El piso vigente, en números** (`§5.6.2`, corrida `a79a9b23676b` con selección prospectiva): WAPE **0,331 / 0,151 / 0,121** (producto / categoría / total, h=1). El retrospectivo daba 0,287 / 0,128 / 0,103 y sigue en `§5.6.1` como registro. **Dos tablas que NO se comparan entre sí:** real contra sintética (composición distinta — 58% `suave` real contra 25% forzado) y retrospectiva contra prospectiva **de otra corrida** (dentro de la misma corrida sí, que es todo el método de §5.6.2).
- **Descompuesto contra el piso anterior** reusando los checkpoints de las dos corridas: **el filtro de obsequios de ADR-012 no movió el piso** (WAPE 0,2939 → 0,2933; el WAPE pondera por magnitud y esas 61 series son diminutas — 0,39% de las unidades). Lo que lo movía era **el mes incompleto**. ADR-012 era necesaria por el deflactor, no por el piso; no la recuerden al revés.
- **Decidido y diferido (2026-08-02):** CP-INF-03 no necesita cubrir todos los peldaños en esta etapa — el laboratorio lo usa 1 producto y ahora se ejercita de costado como nivel de contraste.
- **T0.4 cerrada (2026-07-31):** el generador ya emite `cliente_feature` en 32 versiones, meses de neto negativo y cero, altas y bajas de producto correlacionadas con el arquetipo, y las 12 categorías reales. Cinco `gate_ok` en el manifiesto y **23 tests** — los primeros que el generador tuvo. **Los tests se validaron por mutación:** rompiendo las correcciones caen 7, y uno se borró por seguir verde con el bug puesto. **M2.1 tiene un caso nuevo que cubrir:** 22 filas reales con precio implícito **negativo** por signos cruzados entre unidades y revenue (§5.5 #6), ya sembradas en el sintético.
- **El sesgo de horizonte largo se corrigió dos veces, en direcciones opuestas, y las dos veces por el método de medición.** Primero subió: el −1,4% de la corrida vieja salía de evaluar 1 de los 7 pares de h=12 contra un mes al 32% —achica `real` y corre `pred − real` hacia arriba—, y sacándolo dio −6,0%. Después bajó: con selección prospectiva (M1.9) dio −0,9%, porque el ganador retrospectivo se aplicaba también a los tramos donde ya no era el mejor. **Moraleja para el que siga: antes de convertir un número del piso en decisión de proyecto, preguntá qué convención de medición lo produjo.** ADR-015 se escribió sobre el −6,0% y hay que revisarlo.
- **Cobertura < 1 en el piso real: son DOS causas, no una** (§5.6.1 punto 5). De las 18.355 filas sin predicción **del modelo seleccionado** (6,01%): **69% altas de catálogo** (262 productos con primera venta posterior al corte, 221 de ellos `SIN CATEGORIA` — ningún baseline cubre un producto que no existía) y **31% horizonte truncado** (241 productos que **sí existían**, con 1-11 meses de historia, donde el ganador retrospectivo `SeasonalNaive` **solo proyecta tantos meses como historia tiene** y otros 5-6 candidatos sí predijeron). El segundo componente era **artefacto de la selección retrospectiva y se cerró entero en M1.9**: en el piso prospectivo lo que queda sin cubrir son **12.700 filas, exactamente las altas de catálogo**. El primero no se repara con baselines — es el hueco donde M2 puede ganar con features de categoría/laboratorio. ⚠️ **El "100% son altas de catálogo" que se reportó dos veces era un error de denominador:** contaba solo las filas donde *ningún* candidato predijo. La advertencia genérica del script lo inducía y se corrigió, con test que falla sin el arreglo.
- **Dos trampas del esquema real que el ETL de R1 también se va a comer** (§5.5, pendiente anotado para el Analista): **`producto.id` es `varchar(255)`** y `'2'`/`'02'`/`'0002'` son productos distintos que colapsan al mismo entero (23 colisiones) — castear el sku fusiona productos y ningún test lo nota; **`nota_credito` es `BIT(1)`** y llega como bytes, así que un `== 1` laxo hace que las devoluciones **sumen en vez de restar**.
- **El "13,8% de productos sin venta hace más de 12 meses" NO son bajas.** Con 42% de series intermitentes, un hueco de 12 meses es comportamiento normal. El criterio válido exige que el silencio final **supere el hueco más largo que ese producto ya tuvo estando vivo**, y ahí la tasa real cae a **5,8%**. Vale para cualquier análisis de discontinuados, no solo para el generador.
- **Hallazgo replicado tres veces:** ninguno de los 7 candidatos domina (el mejor se lleva **23%**) y **`CrostonSBA` gana mucho más en `suave` (315) que en `lumpy` (20)** — al revés de la teoría, y ya no es un artefacto del generador. Enrutar por cuadrante habría sido peor que dejarlos competir libres; no enrutar en M2/M3 sin volver a medirlo.
- **Cuidado al correr a escala:** `n_jobs` alto **mata la corrida** por archivo de paginación (`n_jobs=8` murió en el corte 5; usar **4**), y una corrida larga sin `--checkpoint-dir` es una apuesta perdida — el checkpointing ya salvó dos corridas. **Lanzala con `nohup`**: un proceso registrado como tarea de la sesión no sobrevive a un corte de sesión. Costo medido a 2.128 productos: **12→19 min por corte** (sube con la historia), **294 min las 18**. **Y no borres los checkpoints hasta cerrar el análisis**: separar "obsequios" de "mes incompleto" se pudo solo porque los de las dos corridas seguían en disco — se recortan subconjuntos de las predicciones ya hechas y se recalcula, sin reajustar nada.
- Reglas del motor que no hay que romper: **`datasets/` importa del motor, nunca al revés**; clasificar para enrutar exige `hasta=corte`; ninguna tabla se congela en `motor/backtests/` sin `id` de corrida; **los checkpoints del arnés son datos crudos y jamás se commitean** (en M1.8 son datos reales del cliente).
- **Si arrancás una sesión en `motor/` o `datasets/`, leé antes `motor/roadmap-motor.md` §12 y `motor/README.md` §Arranque.** El dataset sintético **no está en el repo** (regenerar por semilla); tres deudas del generador anotadas como **T0.4**.
- Pendientes inmediatos: resolver **P1–P4 del contrato** → congela v1.0; aplicar impacto de **ADR-007/008/009/010/012/013** al DER, CU, Plan de Pruebas y `arquitectura.md` (Analista Funcional — ver `planning/roadmap.md`); correcciones C1–C8 y T1–T6/CP-* al plan de pruebas (Analista Funcional); **ratificar ADR-018 y aplicar su impacto** (PM + Analista) — *reemplaza a ADR-015 y absorbe las tres entradas que estaban abiertas sobre lo mismo; en `planning/roadmap.md` quedan 2 filas en vez de 4*. **Del motor no queda nada abierto: M2 cerrado, M3.0 cerrada en negativo, la deuda de `erratica` cerrada como irreducible. Sigue M3.1.** **Higiene de checkpoints (fuera del repo, son datos crudos — nunca se commitean):** se pueden borrar `C:/dfv-extract` y `-v3`. **NO se borran los dos que sostienen la evidencia vigente:** `C:/dfv-checkpoints-2026-08-03` (los 7 baselines — 294 min re-correrlos; ya respondió preguntas sin re-correr en §5.6.1, §5.6.2 y M2.5) y **`C:/dfv-checkpoints-intervalos`** (el global con cuantiles — respalda M2.4, M2.5, §6.9 y §12.0; 19 min). Prescindibles: `-m30-dispersion` (el negativo de M3.0, ya congelado en tabla), `-m25-sint-*` (sintético, se regenera por semilla) y `-ablaciones*`.
- **Lección de método (2026-07-27, sigue vigente):** "corre sin errores" ≠ "mide bien" — todo test de métrica necesita series dispersas, multi-producto y casos degenerados; un hallazgo sin test ejecutable es una opinión. **Corolario (2026-07-30):** un test que *dice* cubrir un bug tampoco alcanza — verificá que falle sin el arreglo, o es decoración.
