# Datos reales — NUNCA se commitean (ver datasets/README.md)
datasets/*
!datasets/README.md
!datasets/sintetico/
datasets/sintetico/salida/

# Salida de motor/ejemplos/ — exploración, no resultados a versionar
motor/ejemplos/salida/

# Checkpoints del arnés (M1.7a): son el reporte CRUDO fila por fila, no métricas
# agregadas. En M1.8 contienen datos reales del cliente, así que jamás se commitean —
# lo único publicable de un backtest es la tabla de motor/backtests/.
motor/checkpoints/
checkpoints/
corte_*.parquet

*.db
*.sqlite
*.dump

# Secretos y entorno
.env
.env.*
*.pem
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/
*.egg-info/
.pytest_cache/

# Modelos entrenados y experimentos
*.pkl
*.joblib
*.onnx
mlruns/
modelos/

# Node / frontend
node_modules/
dist/
build/

# SO / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
nul
